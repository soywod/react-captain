{
  "private": true,
  "workspaces": ["lib", "storybook"],
  "devDependencies": {
    "prettier": "^1.16.4"
  },
  "dependencies": {
    "typescript": "3.3.3333"
  },
  "scripts": {
    "prepare": "yarn build:lib",
    "start": "yarn workspace react-captain-storybook start",
    "publish": "yarn workspace react-captain publish",
    "build:lib": "yarn workspace react-captain build",
    "build:sb": "yarn workspace react-captain-storybook build"
  }
}
